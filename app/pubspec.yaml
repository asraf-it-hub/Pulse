name: pulse
version: 0.2.0+2
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^8.1.7
  media_kit: ^1.1.11
  media_kit_video: ^1.2.5
  path: ^1.9.0
  shared_preferences: ^2.3.3
  media_kit_libs_video: ^1.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/brand/
